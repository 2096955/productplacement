{
  "name": "ssr-banking-concept-lab-server",
  "version": "1.0.0",
  "private": true,
  "description": "HSBC SSR Concept Lab — backend (Express + Vertex AI via @google/genai)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
